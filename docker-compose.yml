version: '3'
services:
  web:
    build: ./web_app
    ports:
      - "5000:5000"
    environment:
      - REDIS_HOST=redis
    restart: unless-stopped
  app:
    build: ./tasmota_control
    environment:
      - REDIS_HOST=redis
    restart: unless-stopped
  redis:
    image: redis
    ports:
      - "6379:6379"